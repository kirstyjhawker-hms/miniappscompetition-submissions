{
  "name": "submission-ci",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "CI validation for competition submission PRs.",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "download": "cd .. && node scripts/download-repos.mjs"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  }
}
